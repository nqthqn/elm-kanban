# KanBan

A kanban board written in Elm.

## Installation

```sh
$ elm-package install
$ npm install
$ npm run dev
```

## Static assets

Add to `src/assets/` and the production build copies them to `/dist`